{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "agent",
            "prompt": "Use the docs-keeper subagent to check whether CLAUDE.md needs updating based on the file that was just changed.",
            "timeout": 60,
            "async": true
          }
        ]
      }
    ]
  }
}
